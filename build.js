import 'zx/globals';

const fs = require('fs');
const path = './packages';
// require('zx/globals');

// await $`cat package.json | grep name`


// function getDirNames(dir) {
//   return fs.readdirSync(dir);
// }


